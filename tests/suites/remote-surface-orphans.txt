# remote-surface-orphans -- the file list for this step of the root `test` chain.
#
# Read by tests/run-isolated.js --from (see tests/lib/suite-list.js for why
# the list lives in a file and why tools/test-census.js reads it too).
# One repository-relative path per line, in run order. Blank lines and
# lines starting with # are ignored.
tests/remote-agent-bridge.js
tests/remote-bridge-enroll-token.js
# 2026-08-24: the two passing files moved to the green orphan batch. These two
# still depend on undeclared remote machine identities.
