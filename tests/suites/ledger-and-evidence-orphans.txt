# ledger-and-evidence-orphans -- the file list for this step of the root `test` chain.
#
# Read by tests/run-isolated.js --from (see tests/lib/suite-list.js for why
# the list lives in a file and why tools/test-census.js reads it too).
# One repository-relative path per line, in run order. Blank lines and
# lines starting with # are ignored.
tests/queue-from-ledger.js
# 2026-08-24: six passing files moved to the green orphan batch. The live queue
# check keeps its named skip for absent generated queue/owner.ledger.md state.
# 2026-09-02: the one owner request store behind /Request and the Ledger page.
tests/owner-request-store.test.js
tests/rules-turn-snapshot.test.js
tests/ledger-category-reset.test.js
tests/reset-delayed-consumers.test.js
# 2026-09-07: the two audit-record tests added by 09ce9a2 and e302fbc. Both shipped
# without a suite entry, so tools/invocation-guard.js counted them unregistered with
# no invocation path and failed -- taking the whole pretest chain, and every engine
# qualification, down with them. Their modules were already mapped in
# config/packages.json; only this list was missing them.
tests/audit-admission-fallback-sink.test.js
