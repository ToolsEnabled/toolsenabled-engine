# special-session-orphans -- the file list for this step of the root `test` chain.
#
# Read by tests/run-isolated.js --from (see tests/lib/suite-list.js for why
# the list lives in a file and why tools/test-census.js reads it too).
# One repository-relative path per line, in run order. Blank lines and
# lines starting with # are ignored.
# The three dated cross-account migration launchers were retired.  The sealed
# transport remains covered by tests/suites/orphans-wired-0824.txt because FRA
# and link-bus rotation still use that generic cryptographic primitive.
tests/managed-native-tunnel-hostile.test.js
