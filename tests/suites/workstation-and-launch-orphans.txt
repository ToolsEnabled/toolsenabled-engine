# workstation-and-launch-orphans -- the file list for this step of the root `test` chain.
#
# Read by tests/run-isolated.js --from (see tests/lib/suite-list.js for why
# the list lives in a file and why tools/test-census.js reads it too).
# One repository-relative path per line, in run order. Blank lines and
# lines starting with # are ignored.
tests/fra-listener-detached-launch.js
tests/mcp-handshake-probe.js
tests/workstation.js
# 2026-08-24: two passing files moved to the green orphan batch. FRA cleanup is
# reproducibly red, MCP was intermittent after a batch failure, and workstation
# still assumes two machines while the shipped registry declares one.
