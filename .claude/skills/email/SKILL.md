---
name: email
description: Search Gmail and send plain-text email through the authorized Google API adapter.
---

Use `gmail.list` for a narrow query and `gmail.send` to send an email. The adapter
uses a current Google token or refreshes it from encrypted OAuth credentials when
configured.
