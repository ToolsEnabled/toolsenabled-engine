---
name: calendar
description: List or create Google Calendar events through the authorized Google API adapter.
---

Use `calendar.list` to inspect an interval and `calendar.create` with RFC3339 start
and end values. Attendee addresses are optional and are passed only to Google.
